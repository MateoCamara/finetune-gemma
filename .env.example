# Hugging Face access token, used to download the gated google/gemma-2b model
# and to push the fine-tuned model to the Hub.
# Create one at https://huggingface.co/settings/tokens, then copy this file to
# .env and fill in the value below.
HUGGINGFACE_API_KEY=
